import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'progressClock',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
